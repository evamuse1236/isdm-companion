{
  "name": "isdm-companion",
  "version": "1.0.0",
  "private": true,
  "description": "Repository tooling and tests for the ISDM Companion Android beta.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "beta:release": "node scripts/send-beta-release.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
